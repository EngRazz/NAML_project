# Notebook display revision backup

Exact notebook and plot copies made before the approved video naming, 500-episode summaries, and plot layout updates. The saved DDPG outputs belong to a completed post-fix training run. These are provenance copies, not maintained entry points. See `manifest.json` for paths and SHA-256 hashes.
